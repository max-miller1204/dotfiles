[data]
    managedMcpNames = ["nixos", "context7", "playwright"]
    isWSL = {{ if and (eq .chezmoi.os "linux") (or (contains "microsoft" (lower .chezmoi.kernel.osrelease)) (contains "wsl" (lower .chezmoi.kernel.osrelease))) }}true{{ else }}false{{ end }}
