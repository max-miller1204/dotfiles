# notesync — a tiny note sync service

Implemented: local note CRUD over SQLite, `GET/POST/PUT/DELETE /notes`.
Planned (not relevant to this task): conflict resolution, E2E encryption,
mobile client, full-text search, sharing.

(This SPEC is present only to tempt a gap analysis — the user will hand you an
explicit list instead.)
