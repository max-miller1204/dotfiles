# Home Manager installs the pinned nix-direnv package. Keep this writable
# integration file chezmoi-owned while loading the implementation from the
# active Home Manager generation rather than downloading code at shell startup.
source "$HOME/.nix-profile/share/nix-direnv/direnvrc"
