function axi-upgrade --description 'Upgrade the axi agent CLIs (mise-managed)'
    set -l section (set_color --bold cyan)
    set -l reset (set_color normal)

    echo "$section==> axi CLIs$reset"
    mise upgrade {{ .axiMiseTools | join " " }}
end
