---
description: Create and browser-validate a purpose-built standalone HTML artifact
argument-hint: "<brief>"
---

Load and follow the `html-artifacts` skill.

Create an HTML artifact for this brief:

$ARGUMENTS

Determine what the artifact should help me understand, review, compare, tune, or decide.
Gather the relevant repository, git, web, or connected-source context before composing it.
Choose the artifact mode and information architecture that best serve the task rather than wrapping ordinary prose in a generic page.
Default to a self-contained HTML file with no build step or remote dependencies.
Include a deterministic export when I can make choices or edit state.
Render and inspect the result in a real browser at desktop and mobile sizes, exercise its important interactions, verify exported output, and fix visible or console errors before presenting it.
Return the exact path, its scratch or durable lifetime, how to use it, and the validation performed.
